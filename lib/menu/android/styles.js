import React, {
    StyleSheet
} from 'react-native';

export default StyleSheet.create({
    menu: {
        flex: 1,
        padding: 10,
        justifyContent: 'flex-start',
        alignItems: 'center',
        backgroundColor: '#F5FCFF',
    }
});
